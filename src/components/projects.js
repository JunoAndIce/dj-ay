function Projects() {

    return (
        <section ref={menuRef} className="projects w-screen h-screen">
            <div className='flex'>

            </div>
        </section>

    );
}

export default Projects;